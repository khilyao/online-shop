import Navigation from "@components/Navigation";
import Container from "../Container";

const Header = () => {
  return (
    <header>
      <Container>
        <Navigation />
      </Container>
    </header>
  );
};

export default Header;
